# Progress for Willie

## Processed Files

| File | Status | Details |
|------|--------|---------|
| `src/error.rs` | No changes needed | Already clean, minimal, follows Rust conventions. No redundancy or simplification opportunities. |
| `src/lib.rs` | Simplified | Removed redundant `.map_err(Error::Io)` — the `?` operator already uses the `From<std::io::Error>` impl. Rest of file already clean (42 lines, well-structured). |
| `src/main.rs` | Simplified | Collapsed two `output` bindings into one (removed shadowed variable), inlined `elapsed` variable used only once. 62→61 lines. |
| `src/model.rs` | Simplified | Consolidated 4× `std::collections::HashMap`, 1× `std::sync::Arc`, 2× `crate::geometry::*` into top-level imports. Replaced 37-line manual `impl Default for Paragraph` with `#[derive(Default)]` (added `Default` derive + `#[default]` on `Alignment::Left`). Net -34 lines. |
| `src/docx/alt_chunk.rs` | Simplified | 4 changes: (1) `is_table_cell` — `matches!` macro instead of repeated `tag_name().name()` calls; (2) `convert_html_to_blocks` — `unwrap_or_else` instead of 3-line match; (3) `fix_xhtml_void_tags` — hoisted `trim_start()` out of `.any()` closure to avoid redundant re-trimming; (4) `convert_table` — `max_cols.max(total_cols)` instead of if-block. |
| `src/docx/sections.rs` | Simplified | 3 changes: (1) Inlined `ColumnDef` intermediate variables `w`/`sp` directly into struct fields; (2) Replaced 6 mutable `Option` variables + nested match with `find_ref_rid` closure using `find_map` — eliminates mutable state for header/footer RID resolution; (3) Simplified `strip_prefix` path construction with `if let` instead of `map`/`unwrap_or_else`. |
| `src/docx/charts.rs` | Simplified | 6 changes: (1) Extracted `find_srgb_clr` helper to deduplicate `solidFill`→`srgbClr` navigation in `extract_srgb_fill`/`extract_fill_alpha`; (2) Extracted generic `collect_indexed_pts` helper to deduplicate identical structure in `parse_num_cache`/`parse_str_cache`; (3) Extracted `parse_marker_symbol` function from inline closure; (4) Simplified label extraction in `parse_series` using existing `chart_child` helper; (5) Consolidated 6 simple `detect_chart_type` cases into data-driven loop (added `Clone` derive to `ChartType`); (6) Inlined `is_scatter_like` at its single use site. |
| `src/docx/embedded_fonts.rs` | Simplified | 8 changes: (1) Consolidated `std::io::{Read, Seek}` imports; (2) Promoted `embed_variants` to module-level `EMBED_VARIANTS` constant; (3) Simplified `deobfuscate_font` — single `.iter_mut().take(32).enumerate()` loop with `i % 16` instead of two separate 0..16/16..32 loops; (4) Extracted `empty_result` helper to deduplicate 3× `FontTableResult { embedded_fonts: HashMap::new(), font_table }` construction; (5) Extracted `parse_font_family` function from inline closure; (6) Renamed `result` → `embedded_fonts` for field shorthand in struct construction; (7) Simplified `zip_path` construction with `match` on `strip_prefix`; (8) Inlined `font_key` assignment directly into struct literal. |
| `src/docx/headers_footers.rs` | Simplified | 9 changes: (1) Extracted `is_wml_element` helper replacing 4× repeated namespace+name check; (2) Extracted `is_block_element` helper using `matches!` for duplicated `p`/`tbl` guard; (3) Extracted `resolve_alignment` helper eliminating identical 4-line alignment chain in both functions; (4) Replaced inner sdt content loop with `extend` + `filter`; (5) Replaced footnote paragraph loop guard with `filter`; (6) Eliminated duplicate `NumberingInfo::default()` — shared single reference; (7) Hoisted `empty_rels` and `numbering` above loop in `parse_footnotes`; (8) Inlined `space_before`/`space_after`/`borders`/`tab_stops` into struct literal; (9) Simplified final return with `.then(||)`. 191→171 lines. |
| `src/docx/settings.rs` | Simplified | 3 changes: (1) Replaced custom `wml_bool_el` closure with existing `super::wml_bool` shared helper; (2) Replaced manual `defaultTabStop` lookup with `wml_attr(root, "defaultTabStop")`; (3) Replaced manual `themeFontLang` lookup with `wml(root, "themeFontLang")`, inlined booleans into struct literal. 70→49 lines. |
| `src/docx/runs.rs` | Simplified | 9 changes: (1) Promoted `MC_NS` and `REL_NS` to module-level constants (eliminated triple/double definitions); (2) Extracted `mc_choice_or_fallback` helper deduplicating MC Choice/Fallback lookup; (3) Lifted `collect_run_nodes` from nested fn to standalone function; (4) Combined `"ins"` and `"smartTag"` branches with `matches!`; (5) Added `handle_drawing_result!` macro deduplicating identical 24-line `RunDrawingResult` match block; (6) Hoisted `rfonts_node` above `RunFormat` construction to avoid computing it twice; (7) Added `styled_run`/`superscript_run` methods to `RunFormat` eliminating 15 lines of duplicated field listings; (8) Simplified `has_page_break_before` from `let mut` + conditional to direct `let` binding; (9) Inlined `normalized` variable in text handler. 690→678 lines. |
| `src/docx/images.rs` | Simplified | 12 changes: (1) Extracted `emu_to_pts` helper replacing 4× magic `/ 12700.0` division; (2) Extracted `parse_emu_text` helper consolidating optional text→f32→EMU-to-pts pattern; (3) Extracted `wpd` helper analogous to `wml()` but for `WPD_NS` namespace, replacing 3 inline lookups; (4) Extracted `wpd_child_text` helper for finding named child node text under optional parent (used 4×); (5) Extracted `emu_attr` helper for `distT`/`distB` attribute parsing one-liner; (6) Extracted `parse_jpeg_dimensions` function from `image_dimensions` reducing nesting; (7) Used `matches!` macro for JPEG marker check; (8) Simplified PNG magic byte check with slice comparison; (9) Extracted `is_wpd_drawing` helper replacing repeated two-step inline/anchor+namespace check; (10) Removed unnecessary `#[allow(dead_code)]` on `RunDrawingResult`; (11) Simplified `compute_drawing_info` inner loop with `is_wpd_drawing` predicate; (12) Removed redundant docstring on `extent_dimensions`. |
| `src/docx/mod.rs` | Simplified | 9 changes: (1) Consolidated style imports into single `use styles::{...}` statement; (2) Moved `use relationships::parse_relationships` to grouped submodule imports; (3) Extracted `parse_cell_border_with_fallback` helper deduplicating `parse_cell_border_left`/`parse_cell_border_right`; (4) Simplified `font_table_result` destructuring to concise tuple form; (5) Converted `parse_rels_xml` from imperative loop to `filter`+`filter_map`+`collect` iterator chain; (6) Unified indent override logic — consolidated two branches into single tuple expression + one set of conditional assignments; (7) Simplified run color fallback with `get_or_insert`; (8) Simplified tab stops fallback to single `if ... && let Some(...)` expression; (9) Removed unnecessary type annotation on `style_color`. |
| `src/docx/styles.rs` | Simplified | 7 changes: (1) Consolidated `latin_typeface` and `ea_typeface` into single `dml_typeface(node, element)` function (-5 lines); (2) Extracted `parse_font_size` helper replacing 3× `wml_attr(n, "sz").and_then(...).map(|hp| hp / 2.0)` pattern; (3) Extracted `parse_kern` helper replacing 3× identical kern parsing pattern; (4) Extracted `parse_underline` helper replacing 3× `wml(n, "u").and_then(...)` pattern; (5) Extracted `parse_char_spacing` helper replacing 2× spacing parsing pattern; (6) Simplified `inherit!` macro from 23 single-field invocations to variadic macro with one call; (7) Simplified indent extraction from `match`/`Some`/`None` to `.map(...).unwrap_or_default()`, and `.map(|x| f(x))` to `.map(f)`. Net ~-40 lines. |
| `src/docx/numbering.rs` | Simplified | 8 changes: (1) Replaced manual `tag_name().name()` + `tag_name().namespace()` guard for `"lvl"` children with idiomatic `has_tag_name((WML_NS, "lvl"))` filter; (2) Replaced manual `tag_name` guard + triple-nested `if let` chain for `"lvlOverride"` parsing with `filter(has_tag_name)` + `filter_map` iterator chain collected into `HashMap`; (3) Used `wml_attr` instead of `wml` + `.attribute((WML_NS, "val"))` for `label_font_size` parsing; (4) Used `wml_attr` for `label_color` parsing; (5) Used `wml_attr` for `startOverride` value extraction; (6) Flattened nested `if let` chain in `numStyleLink` resolution into early-`continue` `let-else` guards; (7) Removed redundant `ilvl <= prev` guard in counter reset loop (range already empty when `ilvl > prev`), inlined `prev_level` variable; (8) Removed unused `_bullet_font` parameter from private `normalize_bullet_text` and extracted `is_bullet`/`has_pua` locals. 364→341 lines. |
| `src/docx/smartart.rs` | Simplified | 4 changes: (1) Extracted `dsp(node, name)` helper replacing 17× verbose namespace-check-and-find patterns for DSP namespace; (2) Extracted `dml(node, name)` and `has_dml(node, name)` helpers for DML namespace lookups (including `noFill` checks); (3) Extracted `emu_attr(node, attr)` helper consolidating 4× inline EMU-to-pts parse-and-divide chains, promoted `12700.0` to `EMU_PER_PT` constant; (4) Replaced manual `for`/`if let`/`push` loop in `parse_smartart_drawing` with `filter_map(...).collect()`, inlined intermediates. 194→179 lines. |
| `src/docx/tables.rs` | Simplified | 11 changes: (1) Extracted `is_wml` helper replacing 4× verbose namespace+name check pattern; (2) Extracted `margin_twips` helper consolidating left/start and right/end bidi fallback margin parsing; (3) Extracted `border_or_fallback` helper replacing 4× inline border-present-or-fallback blocks; (4) Consolidated duplicate `wml_attr(pr, "tblStyle")` calls — compute `tbl_style_borders` once, derive `has_tbl_style` from it; (5) Used `wml_attr` for `grid_span` instead of manual `.and_then(...).attribute(...)` chain; (6) Simplified `v_align` with direct `match` on `wml_attr`; (7) Simplified `text_direction` with direct `match` on `wml_attr`; (8) Simplified `h_position` with single `match` expression instead of `if let`/`else`; (9) Used `twips_attr` for `row_height` instead of manual parse chain; (10) Simplified `line_spacing` fallback with `.then_some()`; (11) Simplified `space_after` fallback with inline `if`. 335→299 lines. |
| `src/docx/textbox.rs` | Simplified | 8 changes: (1) Extracted `find_dml` and `find_dml_all` helpers eliminating ~25 verbose namespace+name check patterns for DML namespace; (2) Extracted `find_wps` and `find_sp_pr` helpers consolidating WPS namespace lookups and repeated `spPr` search; (3) Unified `resolve_stop_color`, `resolve_scheme_clr_child`, and inline `parse_solid_fill` logic into single `resolve_color_child` function; (4) Eliminated `resolve_scheme_color` by reusing shared `parse_color_transforms` from `styles.rs` (made it `pub(super)`); (5) Extracted `collect_dml_points` helper deduplicating identical point collection in `cubicBezTo`/`quadBezTo`; (6) Extracted `find_wps_style_ref` helper consolidating repeated `wps:style → a:fillRef/a:lnRef` lookup; (7) Extracted `emu_attr` helper replacing repeated EMU-to-points attribute parsing pattern; (8) Minor: `matches!` for VML shape/rect check, hoisted `parse_pt` closure, destructured `dml_pt` as `(x, y)`. 1023→871 lines. |
| `src/fonts/cache.rs` | Simplified | 5 changes: (1) Added `#[derive(Default)]` to `FontCache`, eliminating manual `HashMap::new()` init; (2) Added `use std::fmt::Write` to enable `writeln!` on `String`; (3) Replaced `push_str(&format!(...))` with `writeln!` in `save_cache` — more idiomatic and avoids temporary allocations; (4) Replaced `if face.bold { "1" } else { "0" }` with `u8::from(face.bold)` using standard `From<bool>` impl; (5) Inlined temporary variables in the `"F"` parsing arm of `load_cache` directly into struct literal and `entry()` call. 135→129 lines. |
| `src/fonts/mod.rs` | Simplified | 4 changes: (1) `kern_1000` — replaced four sequential `let Some(...) else` guards with tuple destructure + `.zip().and_then()` chain; (2) `word_width` — eliminated `Vec<char>` collection, iterate directly over `chars()` tracking previous char in `Option`, extracted `scale` variable; (3) `try_font` — extracted `embed` closure capturing shared arguments, flattened nested `if let Some` for embedded fonts into `.and_then()` chain; (4) `register_font` — extracted `try_candidate` closure eliminating three verbose `try_font` call sites, used `find_map` over candidates chained with `.or_else()` for fontTable fallback. 323→257 lines. |
| `src/fonts/discovery.rs` | Simplified | 6 changes: (1) Consolidated imports — merged `HashMap`+`HashSet` and `Path`+`PathBuf` into single `use` statements; (2) Extracted `font_ext` helper deduplicating extension-extraction chain shared by `is_font_file` and `is_font_collection`; (3) Removed `is_font_collection` entirely — `ttf_parser::fonts_in_collection` already returns `None` for non-collection fonts, `unwrap_or(1)` handles both; (4) Used `FontCache::default()` instead of manual struct construction (2 sites); (5) Added `#[derive(Clone)]` to `CachedFace`/`CachedFile` in cache.rs, replacing 10-line manual field-by-field clone with `.clone()`; (6) Used imported `Path` type consistently. 269→242 lines. |
| `src/fonts/embed.rs` | Simplified | 8 changes: (1) Consolidated imports — promoted `Str`, `CidFontType`, `FontFlags`, `SystemInfo`, `UnicodeCmap` and GPOS types to file-level imports; (2) Extracted `to_1000`/`advance_1000` closures deduplicating 5× `value as f32 / units * 1000.0` scaling pattern; (3) Extracted `resolve_glyph` helper consolidating three-tier glyph lookup (standard, PUA fallback, symbol cmap fallback) with `find_map` for symbol cmap; (4) Extracted `extract_kern_pairs` helper for legacy kern table processing, replaced inner loop with `.filter().filter_map().sum()` iterator chain; (5) Extracted `extract_gpos_pairs` helper for GPOS PairAdjustment processing; (6) Extracted `compute_line_metrics` helper for OS/2 vs hhea metric selection with early returns instead of nested if-else; (7) Eliminated duplicate `SystemInfo` construction — built once, reused; (8) Bound `Name(ps_name.as_bytes())` once as `ps_name_ref`, reused across font descriptor, CID font, and Type0 font. Removed 8 WHAT-comments and 1 redundant doc-comment. |
| `src/fonts/encoding.rs` | Simplified | 2 changes: (1) Combined two separate WinAnsi pass-through ranges (`0x0020..=0x007F` and `0x00A0..=0x00FF`) into single match arm using `|`; (2) Replaced manual loop with `push` high/low bytes in `encode_as_gids` with idiomatic `flat_map` + `u16::to_be_bytes()` iterator chain — eliminates mutable `Vec` and manual bit-shifting. |
| `src/geometry/mod.rs` | Simplified | 3 changes: (1) Extracted `scaled_env(w, h)` helper deduplicating identical EMU scaling + `GuideEnv` construction in `evaluate_def` and `evaluate_custom`; promoted magic `12700.0` to `EMU_SCALE` constant; (2) Simplified custom adjustment setup in `evaluate_custom` — replaced per-item loop with single `set_adjustments` call on `custom.adjust_defaults` slice; removed unnecessary `if !adj_overrides.is_empty()` guard; (3) Removed redundant doc comments and a "what" comment on coordinate conversion. |
| `src/geometry/formulas.rs` | Simplified | 5 changes: (1) Extracted `ang_to_rad` and `rad_to_ang` helper functions deduplicating angle conversion formula across `Sin`, `Cos`, `Tan`, and `Atan2` arms; (2) Consolidated three separate `wd`/`hd`/`ssd` builtin loops into single data-driven loop over `(prefix, value, divisors)` table; (3) Simplified `Sin`/`Cos`/`Tan` from 4-line blocks to single-expression arms using `ang_to_rad`; (4) Simplified `CosAtan2`/`SinAtan2` — removed redundant intermediate variables; (5) Simplified `Mod` — replaced three separate `let` bindings with single destructuring tuple. 519→500 lines. |
| `src/geometry/path.rs` | Simplified | 5 changes: (1) Unified `resolve_path` and `resolve_custom_path` duplicated ~80-line command loops into single `resolve_commands` function via private `Cmd<'a>` enum borrowing `&str` from either source; (2) Extracted `quad_to_cubic` helper deduplicating quadratic-to-cubic Bezier conversion formula; (3) Simplified `MoveTo`/`LineTo` branches by assigning directly to `cur_x`/`cur_y` without intermediates; (4) Inlined single-use `cp1x`/`cp1y`/`cp2x`/`cp2y` temporaries in `arc_to_cubics` directly into `CubicTo` constructor; (5) Consolidated `std::f64::consts` imports. 478→410 lines. |
| `src/pdf/mod.rs` | Simplified | 18 changes: (1) Consolidated local imports; (2) Extracted `resolve_h_position` helper deduplicating horizontal position resolution between floating images and textboxes; (3) Simplified `resolve_fi_x` to delegate to new helper; (4) Simplified textbox `tb_x` calculation via shared helper; (5) Replaced 6× fully-qualified `std::collections::HashSet` with imported `HashSet`; (6) Extracted `render_floating_images` helper deduplicating behind-doc/foreground floating image render loops; (7) Simplified behind-doc and foreground rendering blocks via shared helper; (8) Extracted `advance_column_or_page` method on `PageBuilder` consolidating 3× column-overflow/page-break pattern; (9) Replaced 3 inline column/page-advance blocks with method calls; (10) Extracted `styleref_insert_first` helper deduplicating page_first insertion pattern; (11) Simplified `update_styleref_from_para` via shared helper; (12) Used struct destructuring for `EmbeddedImages` instead of 8 individual let bindings; (13) Simplified alpha_set pattern with `if let Some` + `.filter()`; (14) Simplified pattern_refs nesting with `.filter()`; (15) Added `srgb_to_linear_rgb` helper replacing 4× inline array construction; (16) Added `is_at_page_top` method on `PageBuilder` replacing 3× repeated expression; (17) Added model type imports replacing 12× fully-qualified `crate::model::X` references; (18) Removed double blank line. 2472→2436 lines. |
| `src/pdf/layout.rs` | Simplified | 9 changes: (1) Extracted `set_fill_color` helper consolidating 4× inline `if let Some([r,g,b]) / else gray(0)` color-setting pattern; (2) Extracted `set_stroke_color` helper for same pattern on stroke side; (3) Extracted `resolve_tab_aligned_start` helper deduplicating 2× identical tab-alignment resolution match blocks in `build_tabbed_line`; (4) Added `WordChunk::leader` constructor replacing 18-field inline struct literal; (5) Simplified `find_next_tab_stop` from imperative `for`+early-return to `.find().cloned().unwrap_or_else()` iterator chain; (6) Merged two-pass inline image rendering (collect into Vec, then iterate) into single direct-draw loop; (7) Replaced `clear()`+`push_str()` with `clone_from()` for font name update; (8) Imported `Cow` replacing 4× `std::borrow::Cow` fully-qualified paths; (9) Removed unnecessary extra braces block in leader construction. 935→946 lines (net +11 from extracted helpers, offset by deduplication). |
| `src/pdf/table.rs` | Simplified | 11 changes: (1) Extracted `cell_span_width` and `cell_x_offset` helpers deduplicating ~8× col_widths slice computation; (2) Extracted `draw_border` as standalone function replacing 3× identical closure definitions; (3) Extracted `draw_cell_borders` helper consolidating 4-sided border drawing with `draw_top`/`draw_bottom` booleans; (4) Extracted `draw_cell_shading` helper deduplicating cell shading with border inset; (5) Extracted `valign_offset` helper deduplicating 3× vertical alignment calculation; (6) Extracted `para_has_visible_content` and `cell_has_visible_content` replacing 5× inline pattern; (7) Extracted `render_cell_paragraphs` and `render_partial_cell_paragraphs` deduplicating paragraph render loops across 3 render functions; (8) Simplified `draw_cell_label` by extracting `encode_label` and unifying font key lookup; (9) Extracted `render_vertical_cjk_cell` as standalone function from large inline block; (10) Extracted `render_header_rows` and `flush_and_render_headers` closure deduplicating header re-rendering; (11) Consolidated imports. 1147→~1110 lines. |
| `src/pdf/charts.rs` | Simplified | 6 changes: (1) Extracted `DEFAULT_AXIS_COLOR` static replacing 5× hardcoded `[179, 179, 179]` magic value; (2) Simplified legend position extraction in `render_chart` and `render_radar` — replaced 3× `.as_ref().is_some_and()` chains with single `.map(|l| l.position)` + equality checks; (3) Consolidated gridlines and tick marks into single `if let Some(ref val_axis)` block sharing `num_ticks`/`tick_len`/`set_line_width`; (4) Eliminated duplicate coordinate computation in Area chart — compute points once into `Vec<(f32, f32)>`, reuse for fill polygon and stroke outline; (5) Hoisted `set_line_width(0.75)` out of both branches of plot area border conditional; (6) Removed unnecessary `String` allocation for radar "0" label. 1047→1039 lines. |
| `src/pdf/smartart.rs` | Simplified | 4 changes: (1) Replaced inline RGB color conversions with existing `charts::fill_rgb` and `charts::stroke_rgb` helpers, eliminating 3× `r as f32 / 255.0` pattern; (2) Used `match *cmd` instead of `match cmd` with per-field dereferences in `emit_evaluated_paths`; (3) Replaced if/else-if chain with `match (has_fill, has_stroke)` tuple match for fill/stroke painting; (4) Inlined single-use `evaluated` binding in `draw_shape_path` directly into match expression. 153→135 lines. |
| `src/pdf/charts_radial.rs` | Simplified | 5 changes: (1) Imported `FRAC_PI_2` and `TAU` constants replacing 6× fully-qualified `std::f32::consts::*` paths; (2) Promoted magic number `64` to `ARC_SEGMENTS` module-level constant; (3) Extracted `arc_segment_count` helper deduplicating arc subdivision calculation; (4) Extracted `emit_arc` helper deduplicating clockwise arc-tracing loop in `render_pie` and `render_doughnut`; (5) Extracted `emit_arc_reverse` helper for counter-clockwise inner arc in `render_doughnut`. 236→236 lines (net zero — helpers offset by deduplication). |
| `src/pdf/chart_legend.rs` | Simplified | 4 changes: (1) Extracted `item_width` closure deduplicating per-item width formula in `Bottom` placement branch; (2) Hoisted `center_x`, `center_y`, `radius` variables in `render_swatch` shared by `Marker` and `LineMarker` arms; (3) Replaced hardcoded `12.0` with `line_extension(swatch)` call in `LineMarker` arm; (4) Inlined single-use `num_items` binding in `Right` branch. |
| `src/pdf/footnotes.rs` | Simplified | 5 changes: (1) Extracted `substitute_ref_marks` function from inline closure for footnote ref mark replacement; (2) Extracted `layout_paragraph` helper with `ParagraphLayout` struct deduplicating shared sequence (empty check, metrics, line spacing, line building) between `compute_footnote_height` and `render_page_footnotes`; (3) Simplified `compute_footnote_height` from mutable accumulator loop to `filter_map`+`map`+`sum` iterator chain; (4) Simplified `total_fn_height` computation with same iterator pattern; (5) Eliminated redundant `build_paragraph_lines` call by reusing lines from `ParagraphLayout`. |
| `src/pdf/header_footer.rs` | Simplified | 7 changes: (1) Extracted `select_hf` helper deduplicating first/default header-footer selection logic between `effective_slot_top` and `compute_effective_margin_bottom`; (2) Extracted `resolve_tb_y_top` helper for textbox vertical positioning match; (3) Extracted `emit_image_xobject` consolidating repeated save_state/transform/x_object/restore_state pattern for images; (4) Extracted `build_lines` consolidating has_tabs/build_tabbed_line/build_paragraph_lines branching; (5) Replaced inline horizontal positioning with existing `super::resolve_h_position` for textboxes and floating images; (6) Idiomatic Rust: method-syntax `.max()`, single-expression table height, `flat_map` iterator chain; (7) Cleaned up imports. |
| `src/geometry/definitions.rs` | No changes needed | Auto-generated file (15,384 lines, 187 preset shape definitions). Header says "do not edit manually". Source: `tools/src/bin/generate_shapes.rs`. |
