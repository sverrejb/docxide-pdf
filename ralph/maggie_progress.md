# Progress for Maggie — fixtures from Marge's coverage audit

Maggie reads Marge's gap audit (`ralph/marge_progress.md`), picks the next
high-value gap not yet handled here, checks whether any existing fixture already
exercises it, and — only if not — creates a new test fixture. It NEVER produces
`reference.pdf`; that is the user's MS Word export job. New fixtures are parked
in `tests/fixtures/SKIPLIST` until the user supplies the reference.

## Ledger

One row per gap Maggie has processed. Status values:
- **COVERED** — an existing fixture already exercises this element; no new fixture.
- **FIXTURE-PENDING-REF** — new fixture created, but its `reference.pdf` does not exist yet (not ready for Homer).
- **READY** — new fixture created AND its `reference.pdf` exists; ready for Homer to implement.
- **SKIPPED** — no print impact / not worth a fixture (say why).

| Gap (spec ref + element) | Priority | Status | Fixture / note |
|---|---|---|---|
| 17.3.2.24 `w:position` (vertically raised/lowered text) | HIGH | COVERED | `tests/fixtures/scraped/polish_building_procurement_spec` exercises it with a real non-zero `<w:position w:val="12"/>` (+6pt raise) in `document.xml`; `czech_municipal_grant_form` has `w:val="-1"` in styles. (3 other fixtures carry only no-op `w:val="0"`.) No new fixture. |
| 17.3.1 `w:textAlignment` (vertical char alignment on line) | HIGH | COVERED | Real non-default values present in the corpus (in `word/styles.xml` pPr): `mongolian_human_rights_law` 11× `top`; `transition_to_work_deed` 6× `center` + 4× `top`; `russian_sports_ranking_decree` 9× `center`; `czech_municipal_grant_form` 2× `top`; `italian_evaluation_minutes` 2× `center`. (10 fixtures total contain the element; document-level instances elsewhere — east_asia/korean_japanese/lithuanian/usep — are all the implicit-default `baseline`/`auto`.) No new fixture. |
| 17.3.2.23/.31/.13/.18 run text-effect toggles `w:outline`/`w:shadow`/`w:emboss`/`w:imprint` + `w:effect` (ST_TextEffect) | HIGH | READY | Corpus has only no-op instances (`traditional_skills_job_form` styles.xml: `w:outline`/`w:emboss`/`w:imprint` all `w:val="0"`; `croatian`/`czech` `w:effect w:val="none"`) — none activates the toggle, so the feature is genuinely unexercised. New fixture `tests/fixtures/cases/case66` isolates all 5 as active run toggles (bare `<w:outline/>` etc. + `<w:effect w:val="shimmer"/>`), one per paragraph, 28pt bold. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.5.1.1/.3/.5/.6 `w:customXml` (block / cell / row / inline transparent wrapper) | HIGH | READY | `customXml` appears NOWHERE in `src/` and 0 fixtures exercise it (`analyze-fixtures --grep "w:customXml"` → 0) — content wrapped solely in `w:customXml` is silently dropped (the descent helpers special-case only `w:sdt`/`w:smartTag`/`mc:AlternateContent`). New fixture `tests/fixtures/cases/case67` puts text that exists ONLY inside `w:customXml` wrappers at all four placements (block paragraph, inline run, cell `w:tc`, whole-row `w:tr`); reference reveals whether each placement survives. Injected via ZIP post-processing (case66 pattern). reference.pdf present; un-skiplisted, runs in the suite. |
| 17.6.10 `w:pgBorders` + .2/.7/.15/.21 `w:top`/`w:left`/`w:bottom`/`w:right` (Page Borders) | HIGH | READY | `grep "pgBorders" src/` → 0 hits and `analyze-fixtures --grep "w:pgBorders"` → 0 fixtures — no model field, parse, or render; a page-border box (common on title pages/certificates/forms) draws nothing today. CT_Border per-edge decode already exists for paragraph/table borders but the page-level container + page placement are missing. New fixture `tests/fixtures/cases/case68` isolates a plain-line border box: four CT_Border children (`single`, sz=24 → 3pt, space=24, color `1F4E79`) under `<w:pgBorders w:offsetFrom="page">`, injected into the default `w:sectPr` before `w:cols` (CT_SectPr child order) via ZIP post-processing. Art (image) borders deliberately out of scope. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.6.23 `w:vAlign` (Vertical Text Alignment on Page, section level) | HIGH | READY | `grep "vAlign" src/` hits only the cell-level `w:vAlign` (`src/docx/tables.rs`); the section-level `sectPr` child is unparsed and body text is always top-aligned. `analyze-fixtures --grep "w:vAlign"` → 15 fixtures but all are cell-level (`w:tcPr`); 0 use it inside `w:sectPr` (Marge verified by scanning sectPr segments) — latent gap. New fixture `tests/fixtures/cases/case69` injects `<w:vAlign w:val="center"/>` into the default `w:sectPr` after `w:cols` / before `w:docGrid` (CT_SectPr child order) via ZIP post-processing; tiny centered title + body so center-vs-top placement is unmistakable in the reference. `both`/`bottom` variants deliberately left for later. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.6.8 `w:lnNumType` (Line Numbering Settings, section level) | HIGH | READY | `grep "lnNumType" src/` → 0 hits (not modeled, parsed, or rendered) and `analyze-fixtures --grep "w:lnNumType"` → 0 fixtures — margin line numbers (legal/contract/code listings) draw nothing today. New fixture `tests/fixtures/cases/case70` injects `<w:lnNumType w:countBy="1" w:start="1" w:restart="continuous"/>` into the default `w:sectPr` before `w:cols` (CT_SectPr child order) via ZIP post-processing; 12 short single-line paragraphs so the 1,2,3… left-margin sequence is unmistakable in the reference. `@distance` and the paired per-paragraph `w:suppressLineNumbers` (17.3.1.35) deliberately left for later. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.9.4 `w:isLgl` (Display All Levels Using Arabic Numerals — "legal numbering") | HIGH | READY | `grep isLgl src/` → 0 hits (no `LevelDef` field, never parsed); `analyze-fixtures --grep` can't see `numbering.xml`, so scanned all 6 fixtures whose `numbering.xml` carries `isLgl` (feminist_voice_dissertation, indonesian_benchmarking_guide, education_consultant_posting, russian_construction_org_council_minutes, ukrainian_municipal_heating_resolution, polish_municipal_notification) and cross-checked each `isLgl` level's `lvlText` `%N` references against the referenced levels' `numFmt` AND against the `(numId,ilvl)` pairs actually used by `w:numPr` in `document.xml`. EVERY `isLgl` level references only `decimal` levels (`NONDECIMAL_REFS=[]` in all cases) — these are Word's auto `multilevel` legal templates that are all-decimal by construction, so `isLgl` forcing decimal is a visual no-op (the same trap as `w:position w:val="0"` / tentative `lvlJc`). Only ONE `isLgl` level is even used by a paragraph (indonesian abs26 ilvl2, `%1.%2.%3`, all decimal). So the feature — forcing a NON-decimal referenced level to render decimal — is genuinely unexercised. New fixture `tests/fixtures/cases/case71` isolates it: ilvl 0 `upperRoman` `%1.` → "I."/"II."; ilvl 1 `decimal` + `<w:isLgl/>` `%1.%2.` → with `isLgl` the `%1` is forced decimal so subs read "1.1."/"1.2."/"2.1." (a renderer ignoring `isLgl` shows "I.1."). Custom abstractNum/num id 100 injected into `word/numbering.xml` via ZIP post-processing (case43/numbering-injection pattern). reference.pdf present; un-skiplisted, runs in the suite. |
| 17.9.10 `w:lvlRestart` (Restart Numbering Level) | HIGH | READY | `grep lvlRestart src/` → 0 hits (not parsed; `parse_list_info` implements only the default cascade-restart). `analyze-fixtures --grep` can't see `numbering.xml`, so scanned every fixture's `numbering.xml` directly AND cross-checked each `lvlRestart` level against the `(numId, ilvl)` pairs used by `w:numPr` in `document.xml`. 6 fixtures carry it but all are no-ops: the `w:val="0"` (continuous) instances — including victorian_universal_design_policy 45×, uk_commercial_lease_template 37×, western_australia_owner_drivers_act 9× — sit on levels NO paragraph uses; the only USED level with an explicit value (uk_commercial_lease numId 34/39/… **ilvl 2**, `lvlRestart="1"`) never triggers because those docs use ONLY ilvl 2 — no parent (ilvl 0/1) paragraph ever advances, so neither default nor gated restart fires (same boilerplate trap as `isLgl`/`lvlJc`). So the feature is genuinely unexercised. New fixture `tests/fixtures/cases/case72` isolates the visible `lvlRestart="0"` case: ilvl 0 `decimal` `%1.` (sections "1."/"2."); ilvl 1 `decimal` + `<w:lvlRestart w:val="0"/>` `(%2)` → sub-items run continuously (1)(2)(3)(4) across the Section-2 heading instead of restarting (1)(2)(1)(2). Custom abstractNum/num id 100 injected into `word/numbering.xml` (case71 pattern). The gated `lvlRestart="K"` variant (restart only on a specific ancestor) deliberately left for later. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.9.23 `w:pStyle` (Paragraph Style's Associated Numbering Level — style→level reverse lookup) | HIGH | READY | `<w:lvl>/<w:pStyle>` is present in nearly EVERY fixture, but the feature (pStyle picking a level that differs from what the code computes) is genuinely unexercised. Scanned every fixture's `numbering.xml`+`styles.xml`+`document.xml` directly (`--grep` can't see `numbering.xml`): for each USED paragraph style mapped via a level's `pStyle`, checked the named level index and whether an explicit `ilvl` exists (style numPr or paragraph numPr). EVERY real usage is latent — styles named on **level 0** carry no `ilvl` but default-0 is already correct (croatian Naslov1, uk_lease SHHeading1/SHScheduleHeading, master_thesis Heading2, covid IOPRefs, case33 ListBullet, transition StandardClause/ListparaAddendum, victorian Tablebullet1), and styles named on **level >0** (Naslov2 lvl1, SHHeading2-5 lvl1-4, SHScheduleText1-5, StandardSubclause lvl1, Tablebullet2 lvl1) ALL write an explicit matching `ilvl` in their style numPr (pStyle redundant). Across ALL fixtures, ZERO map a used style to a level >0 with no explicit `ilvl` — so today's renderer (defaults absent ilvl to 0, `styles.rs:679`) never produces wrong output. New fixture `tests/fixtures/cases/case73` isolates it: 3 styles PHeadingA/B/C each with `<w:numPr><w:numId w:val="100"/></w:numPr>` and NO ilvl, mapped by the abstractNum's per-level `pStyle` to lvl0 `upperRoman %1.`, lvl1 `lowerLetter %2.`, lvl2 `lowerRoman %3)`. Correct (Word) → I./a./b./II./a./i); a renderer ignoring pStyle collapses all to lvl0 → I.II.III.IV.V.VI. abstractNum/num id 100 + the 3 styles injected via ZIP post-processing (case71 numbering pattern + styles.xml append). reference.pdf present; un-skiplisted, runs in the suite. |
| 17.11.12/.4 `w:footnotePr` / `w:endnotePr` (Document-Wide note property bag) | HIGH | READY | `grep footnote\|endnote src/docx/settings.rs` → 0 hits (the doc-wide property bag in `word/settings.xml` is never parsed; `Document` has no note-property field — `numFmt`/`pos`/`numStart`/`numRestart` all fall back to the hardcoded decimal/pageBottom/docEnd defaults in `src/pdf/footnotes.rs`+`mod.rs`). `analyze-fixtures --grep "w:footnotePr"` reports 1, but that scans only `document.xml`+`styles.xml`; scanned every fixture's `word/settings.xml` directly — ~75 carry a `footnotePr`/`endnotePr`, but ALL contain ONLY the special-note ID list (`<w:footnote w:id="-1"/>` etc.), **0 carry a `numFmt`/`pos`/`numStart`/`numRestart` child** (the Word-default bag has no print-affecting child here, same no-op trap as empty `<w:footnotePr/>`). So a doc-wide bag that actually changes note numbering is genuinely unexercised. New fixture `tests/fixtures/cases/case74` isolates it: `word/settings.xml` sets `<w:footnotePr><w:numFmt w:val="upperRoman"/></w:footnotePr>` (footnote marks → I, II, III) and `<w:endnotePr><w:numFmt w:val="lowerLetter"/></w:endnotePr>` (endnote marks → a, b); 3 footnote refs + 2 endnote refs in a tiny doc — today's renderer prints both as plain decimal, so the reference reveals whether the bag is honored. Built from raw XML (case18 footnote pattern; python-docx can't author notes) + an endnotes part (the corpus has none). `pos`/`numStart`/`numRestart` and the section-wide override (17.11.11/.5) deliberately left for later. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.11.11/.5 `w:footnotePr` / `w:endnotePr` (Section-Wide note property bag, in `w:sectPr`) | HIGH | READY | `grep "footnote\|endnote" src/docx/sections.rs` → 0 hits; `SectionProperties` has no note-property field, so a `footnotePr`/`endnotePr` child of `w:sectPr` is never parsed (distinct from case74, which sets the DOC-WIDE bag in `word/settings.xml`). `analyze-fixtures --grep` scans only `document.xml`+`styles.xml` and can't isolate sectPr-vs-settings placement; Marge verified by scanning sectPr segments that 0 fixtures carry a `footnotePr`/`endnotePr` *inside* `w:sectPr` (all corpus instances are the settings-part doc-wide bag, themselves all no-op ID-list-only — same trap as case74's doc-wide finding) — latent gap. New fixture `tests/fixtures/cases/case75` isolates it: the section-wide bags live in the body `w:sectPr` (CT_SectPr child order: `footnotePr?`/`endnotePr?` FIRST, before `pgSz`) with NON-DEFAULT formats and NO `word/settings.xml` bag at all, so the sectPr is the ONLY format source — `<w:footnotePr><w:numFmt w:val="upperRoman"/></w:footnotePr>` (footnote marks → I, II, III) + `<w:endnotePr><w:numFmt w:val="lowerLetter"/></w:endnotePr>` (endnote marks → a, b); today's renderer prints both plain decimal, so the reference reveals whether the sectPr-level numFmt is honored. Raw-XML ZIP pattern (case74/case18). `pos="sectEnd"` + multi-section override deliberately left for the 17.11.21/.22 placement fixture. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.11.18/.17 `w:numFmt` (Footnote / Endnote Numbering Format) | HIGH | COVERED | This is the numbering-FORMAT child of the footnote/endnote property bag — exactly the element my own case74 + case75 already isolate. Verified via `docx-inspect`: `tests/fixtures/cases/case74` (doc-wide, `word/settings.xml`) AND `tests/fixtures/cases/case75` (section-wide, body `w:sectPr`) both carry `<w:footnotePr><w:numFmt w:val="upperRoman"/></w:footnotePr>` (marks → I, II, III) + `<w:endnotePr><w:numFmt w:val="lowerLetter"/></w:endnotePr>` (marks → a, b). Corpus has 0 non-no-op `numFmt` in any footnote/endnote bag (Marge: all ~75 corpus bags are ID-list-only), so these two pending fixtures are the only exercise — and they cover it. No new fixture. (`w:pos`/`numStart`/`numRestart` are separate gaps.) |
| 17.11.21/.22 `w:pos` (Footnote / Endnote Placement, ST_FtnPos/ST_EdnPos) | HIGH | READY | Footnotes are always painted at the page-bottom margin (`render_page_footnotes`, `src/pdf/footnotes.rs`; `grep "\"pos\"" src/docx/settings.rs` → 0 — the footnote bag is unparsed); `beneathText`/`sectEnd`/`docEnd` are never honored. `analyze-fixtures --grep "w:pos "` → 0 fixtures (the `w:pos="720"` hits are all `w:tab` positions, unrelated); the ONLY corpus `footnotePr` (`federal_procurement_terms`, section-wide) carries `<w:numRestart w:val="eachPage"/>` with NO `w:pos` child — so footnote/endnote placement is genuinely unexercised (case74/case75 explicitly deferred `pos` to "the 17.11.21/.22 placement fixture"). New fixture `tests/fixtures/cases/case76` isolates the clearest single-page case: doc-wide `<w:footnotePr><w:pos w:val="beneathText"/></w:footnotePr>` with a SHORT body near the top of a tall page, so Word renders the 2 footnotes hugging the text block while today's docxside pins them to the bottom margin — an unmistakable divergence. Numbering left at default decimal so PLACEMENT is the only variable. Raw-XML ZIP pattern (case74/case18); python-docx can't author notes. Endnote `pos="sectEnd"` (needs a multi-section doc to differ from the docEnd default) deliberately left for later. reference.pdf present; un-skiplisted, runs in the suite. |
| 17.15.1.10 `w:autoHyphenation` (+ .53 `hyphenationZone` / .22 `consecutiveHyphenLimit` / .37 `doNotHyphenateCaps`) — hyphenation cluster | HIGH | COVERED | `analyze-fixtures --grep` scans only `document.xml`+`styles.xml` so can't see these settings-part elements; scanned every fixture's `word/settings.xml` directly. The cluster is genuinely exercised — and there's a DEDICATED purpose-built isolation set `tests/fixtures/hyphenation/{english,german,french,spanish,norwegian,dutch,italian,slovak}` that injects bare `<w:autoHyphenation/>` into justified, narrow-margin Wikipedia text per language AND already ships Word `reference.pdf` for all 8. Corpus also covers it: `autoHyphenation` true in `german_mezzo_soprano_bio` (`val="true"`), `slovak_misdemeanor_amendment` (bare), `traditional_skills_job_form` (`val="1"`); `hyphenationZone` in ~40 fixtures (e.g. slovak_misdemeanor `425`, italian_evaluation `283`, bosch_software_ai `142`); `doNotHyphenateCaps` in ~7 (bush_fires_act_comparison, centrifugal_water_chillers, english_town_council_report, lithuanian_excise_tax/railway, americas_counter_terrorism, lithuanian_food_quality_order). Only `consecutiveHyphenLimit` (17.15.1.22) has 0 corpus instances — but it's a no-op modifier on a hyphenation engine docxside doesn't yet run, and the cluster's primary visual feature (autoHyphenation line-break/hyphen insertion) is fully isolated with references. No new fixture. |
| 17.9.7 `w:lvlJc` (Numbering Symbol Justification, `center`/`end`/`right`) | HIGH | COVERED | `analyze-fixtures --grep` can't see `numbering.xml`, so scanned every fixture's `numbering.xml` directly AND cross-checked each level against the `(numId, ilvl)` pairs actually referenced by `w:numPr` in `document.xml` (mapping `num`→`abstractNum`). The `w:val="right"` that appears in ~half the corpus is mostly the Word `hybridMultilevel` `lowerRoman` boilerplate on `w:tentative="1"` levels that no paragraph uses (e.g. handcrafted `case3` carries `right` on ilvl 2/5/8 but only ever uses ilvl 0) — those are no-ops, like the `w:position w:val="0"` trap. Genuinely USED non-default-`lvlJc` levels DO exist: `tests/fixtures/new/ukrainian_municipal_heating_resolution` (numId 28, **ilvl 0**, `jc=right`, lvlText `%1.` — a top-level right-justified decimal list, the clearest visual test); `tests/fixtures/new/construction_bathroom_accessories_spec` (numId 2, used 42×, `jc=right` on ilvl 2 `%3.` + ilvl 5 `%6.`); `tests/fixtures/.../centrifugal_water_chillers` (numId 7/10, ilvl 4, `jc=right`, `%5)`). No genuinely-used `center` instance found, but `right` (the spec's primary case — right-aligned legal/outline/TOC numbers) is exercised. No new fixture. |
