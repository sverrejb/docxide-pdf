# Progress for Homer — implement features to make Maggie's cases render right

Homer reads Marge's gap detail (`ralph/marge_progress.md`) and Maggie's case
ledger (`ralph/maggie_progress.md`), then works ONE case per iteration: implement
the rendering feature the case isolates, score it against its Word `reference.pdf`,
and commit on improvement. It works only on Maggie's FIXTURE-PENDING-REF cases.

## Cases

Status values:
- **DONE** — crosses thresholds (Jaccard ≥20% / SSIM ≥75%), committed, removed from SKIPLIST.
- **IMPROVED** — score went up but still below threshold (committed, left in SKIPLIST).
- **BLOCKED** — can't fix now; note why and what was learned.
- **IN-PROGRESS** — started, not finished.

| Case | Gap / feature | Status | Score (Jaccard / SSIM) | Notes |
|---|---|---|---|---|
| case66 | Run text-effect toggles `w:outline`/`w:shadow`/`w:emboss`/`w:imprint` + `w:effect` (§17.3.2.23/.31/.13/.18) | BLOCKED | 63.1/60.4 → 74.1/70.6 → 74.9/71.1 | Parsed all four bare toggles via `wml_bool` in `src/docx/runs.rs`; `w:effect` (animated shimmer) intentionally ignored — no print form. `outline` → hollow glyph (`TextOutline` stroke + `TextFill::NoFill`, hairline width); `shadow` → light gray drop shadow down-right (offset = 0.035·fontSize); `emboss`/`imprint` → gray drop shadow at the SAME offset (this run: changed from 0.7× to full offset in `legacy_text_shadow`, `src/docx/runs.rs` — emboss/imprint now visually match the reference's shadow prominence; +0.8 Jaccard / +0.5 SSIM, no regressions, 197 unchanged). BLOCKED: SSIM stalls at 71.1 vs the 75 threshold. The diff image (`tests/output/cases/case66/diff`) shows title/`Shadow`/`Shimmer`/`Outline` all match (gray), but `Emboss`/`Imprint` glyph BODIES are heavily blue (reference-only ink) — i.e. Word renders those legacy reliefs with a heavier/darker glyph body, not a separated drop shadow. The ONLY lever to add body ink is face-thickening, which the prior run already proved *lowers* SSIM to 68% (the stroke overshoots matching edges into red). So the single-pass black-face + offset-gray-shadow model is at its ceiling for emboss/imprint; Word's multi-pass face/highlight/shadow antialiasing isn't pixel-replicable cheaply. Jaccard clears 20% by 55pp. Marked BLOCKED (not IMPROVED) so the loop advances to the genuinely sub-threshold cases (case73 SSIM 64.1, case75 SSIM 62.2). |
| case67 | `w:customXml` transparent wrapper at block / inline-run / cell / row placements (§17.5.1.1/.3/.5/.6) | DONE | 25.0/26.1 → 71.8/91.3 (TxtBnd 0→100) | `w:customXml` is a transparent content wrapper whose children ARE the content (no `sdtContent` nesting, unlike `w:sdt`). Two edits: (1) `collect_block_nodes` in `src/docx/mod.rs` now recurses into `w:customXml` children — one fix covers block (body), row (tbl→tr) and cell (tr→tc) placements since tables already descend via `collect_block_nodes`; (2) `collect_run_nodes` in `src/docx/runs.rs` adds `customXml` to the `ins`/`smartTag` passthrough arm for the inline-run placement. Previously this content was silently dropped. Crosses both thresholds. Full suite: no regressions (197 unchanged). |
| case69 | `w:vAlign` section-level vertical page alignment (§17.6.23, ST_VerticalJc `center`/`bottom`) | DONE | 0.0/1.7 → 88.1/96.2 (TxtBnd 100) | Section body text was always top-aligned; the `sectPr` child `w:vAlign` was unparsed. Added `PageVerticalAlign` enum + `vertical_align` field to `SectionProperties` (`src/model/mod.rs`); parsed `w:vAlign @val` (center/both/bottom/top) in `src/docx/sections.rs`. Rendering: `PageBuilder` now records each page's content-bottom y at flush (`all_content_bottom`, `src/pdf/mod.rs`); `render()` computes a per-page downward offset (`slack = content_bottom − region_bottom`; center = slack/2, bottom = slack) and `assemble_pdf_pages` wraps ONLY the body content stream in a `q 1 0 0 1 0 −off cm … Q` translate (mirroring the existing comment-scale wrapper), leaving header/footer/page-border streams page-fixed. KEY fix: `slot_top` at flush excludes the last paragraph's trailing `space_after`, but Word's centered content box INCLUDES it — subtracting `prev_space_after` from the final page's content-bottom corrected a uniform 4.8pt over-shift (took Jaccard 19.8→88.1, SSIM 48.4→96.2). `both` (vertical justify) intentionally falls back to Top (inter-paragraph distribution unexercised). Crosses both thresholds. Full suite: no regressions (197 unchanged). |
| case68 | `w:pgBorders` page-border box + per-edge `w:top`/`w:left`/`w:bottom`/`w:right` (§17.6.10 / .2/.7/.15/.21) | DONE | 18.4/31.2 → 91.9/94.6 | Page borders were entirely unmodeled (the whole box drew nothing). Added `PageBorders` + `PageBorderDisplay` to `SectionProperties` (`src/model/mod.rs`); parsed `w:pgBorders` in `src/docx/sections.rs` reusing the existing `parse_one_border` CT_Border decode for each edge (`@val`/`@sz`/`@space`/`@color`) plus `@offsetFrom` (page vs text) and `@display` (allPages/firstPage/notFirstPage); rendered in `src/pdf/assembly.rs::render_page_borders` as four independently-stroked edges (corners closed by overrunning horizontals by the vertical half-width), emitted per page in unscaled page coords right after the header/footer block (before the comment-scale wrapper). `@offsetFrom="page"` → stroke centered at `space + width/2` inward from the page edge (spec §17.18.63: space = page-edge-to-border distance); `text` path insets outward from the margins (ponytail-commented, not exercised by this fixture). `@display` gated on `is_first_page_of_section`. Crosses both thresholds. Full suite: no regressions (197 unchanged). Art (image) borders + `alignBordersAndEdges` deliberately out of scope. |
| case71 | `w:isLgl` legal numbering — force every `%N` reference in a level's `lvlText` to decimal regardless of the referenced level's `numFmt` (§17.9.4) | DONE | 80.2/89.5 (TxtBnd 50) → 87.4/96.6 (TxtBnd 100) | Added `is_lgl: bool` to `LevelDef`, parsed via `wml_bool(lvl, "isLgl")` in `parse_level_def` (`src/docx/numbering.rs`). In the label-substitution loop, when the *current* level's `is_lgl` is set, every `%N` placeholder is formatted with `decimal` instead of the referenced level's own `num_fmt` — so the fixture's lvl1 (`decimal`+`isLgl`, lvlText `%1.%2.`) renders `1.1.`/`1.2.`/`2.1.` instead of the `I.1.`/`I.2.`/`II.1.` it produced before (lvl0 stays `upperRoman` → `I.`/`II.`). TxtBnd 50→100 confirms the sub-item labels now match Word exactly. Added unit test `test_islgl_forces_referenced_levels_decimal`. Crosses both thresholds. Full suite: no regressions (197 unchanged). NOTE: change is complete + verified in the working tree (`src/docx/numbering.rs`) but UNCOMMITTED — this session's sandbox denies all `.git/` writes and `git commit`/`git add` aren't on the outside-sandbox allowlist (only `git diff`/`checkout`/`stash` are), so the commit needs the user to run it or grant the permission. |
| case72 | `w:lvlRestart` restart-numbering-level (§17.9.10, `@val`=0 never-restart / `@val`=K gated restart) | DONE | committed-main wrong `(1)(2)(1)(2)` → 84.2/96.0 (TxtBnd 100) | Default cascade-restart was unconditional: `parse_list_info` (`src/docx/numbering.rs:365`) cleared every deeper counter on return to a higher level, so a continuous sub-list restarted incorrectly. Added `lvl_restart: Option<u32>` to `LevelDef`, parsed via `wml_attr(lvl, "lvlRestart")` in `parse_level_def`. The reset loop now gates each deeper level on its own `lvl_restart`: `Some(0)` → never reset (continuous); `Some(k)` (k ≤ deeper+1) → reset only when the trigger level `ilvl < k` (the gated "restart on level k or earlier" variant — implemented, not deferred); `None` → default restart-on-any-earlier. For case72's lvl1 `lvlRestart=0` the sub-items now run `(1)(2)(3)(4)` across the Section-2 heading (was `(1)(2)(1)(2)`), matching Word exactly (TxtBnd 100, mutool text identical to reference). Added unit test `test_lvlrestart_zero_keeps_continuous_numbering`. Crosses both thresholds. Full suite: no regressions (197 unchanged; `uk_commercial_lease` hash shifted sub-pixel but Jaccard/SSIM/TxtBnd byte-identical — its `lvlRestart=1` levels never trigger, doc uses only ilvl 2). NOTE: the numbering.rs working-tree change bundles case71's `isLgl` (already DONE) in the same hunks. |
| case73 | `w:pStyle` numbering-level / paragraph-style association (§17.9.23) | DONE | 51.6/64.1 (TxtBnd 28.6) → 90.3/95.6 (TxtBnd 100) | **Maggie predicted REVERSE-lookup (style→level: PHeadingB→lvl1 "a.", PHeadingC→lvl2 "i)"), but the Word `reference.pdf` shows the OPPOSITE: only PHeadingA paragraphs number (I./II.); PHeadingB & PHeadingC show NO number and sit at the lvl0 hanging position.** So `w:pStyle` in a `w:lvl` is a *display GATE / level-owner marker*, not a level selector: the level still comes from the numPr ilvl (here absent → default 0 for all three styles), and the number renders only if the resolved level's `pStyle` names the paragraph's effective style. Implemented in `src/docx/numbering.rs`: parse `pStyle` per level into `LevelDef.pstyle`; added `effective_style_id` param to `parse_list_info` (passed as `para_style_id` from `paragraph.rs` + `tables.rs`). When numbering is *style-inherited* (`num_pr.is_none()`) and the resolved level's `pStyle` ≠ the paragraph's style, return early with the level's indent but an EMPTY label and NO counter advance — so PHeadingB/C keep lvl0 indent (first line at hanging pos 14.4pt, matching the reference) and the lvl0 counter goes I.→II. (not III.) across them. Direct paragraph numPr is NOT gated (explicit author choice). Added unit test `test_pstyle_gates_number_to_owning_style`. Crosses both thresholds. Full suite: 197 unchanged, no regressions (`uk_commercial_lease` hash shifted sub-pixel: Jaccard/SSIM −0.02/−0.04pp, TxtBnd byte-identical — its heading styles directly match their level's pStyle so the gate passes). ponytail: gate matches the level's named style directly, not styles `basedOn` it. |
| case74 | `w:footnotePr`/`w:endnotePr` note mark numbering format (§17.11.18/.17 `w:numFmt`) — doc-wide settings.xml bag | DONE | 66.0/83.2 (TxtBnd 55.6) → 66.3/83.6 (TxtBnd 100) | **Maggie predicted "honor the settings.xml numFmt" (footnotes upperRoman I/II/III, endnotes lowerLetter a/b). The Word `reference.pdf` shows the OPPOSITE: footnotes render `1,2,3` (decimal) and endnotes `i,ii` (lowerRoman) — Word IGNORES the doc-wide `word/settings.xml` footnotePr/endnotePr numFmt and uses its built-in defaults.** Proof of the real source: scraped fixture `erasmus_plus_staff_mobility_agreement` (7 endnotes) renders DECIMAL endnotes and carries `<w:endnotePr><w:numFmt w:val="decimal"/></w:endnotePr>` in its **body `w:sectPr`** (not just settings.xml), while case74's `w:sectPr` is EMPTY and its settings.xml `lowerLetter` is dropped. So Word reads the mark format from the SECTION-level `w:sectPr` footnotePr/endnotePr (§17.11.11/.5), defaulting footnote=decimal / endnote=lowerRoman when absent. Implemented: added `footnote_num_fmt`/`endnote_num_fmt: Option<String>` to `SectionProperties` (`src/model/mod.rs`), parsed `w:sectPr/w:footnotePr|w:endnotePr / w:numFmt @val` in `src/docx/sections.rs`; the footnote/endnote display-order pre-pass (`src/pdf/mod.rs`) now formats each sequential mark via `numbering::format_number` with `fn_fmt = first section's footnotePr numFmt else "decimal"`, `en_fmt = … else "lowerRoman"` (maps changed `HashMap<u32,u32>`→`<u32,String>`; `substitute_ref_marks` in `src/pdf/footnotes.rs` takes `&str`). First attempt (honoring settings.xml) REGRESSED case74 to TxtBnd 11.1 and erasmus −3.5pp — proving the settings-bag is wrong; the section-level model fixes case74 (TxtBnd 55.6→100, endnotes now `i,ii`) and restores erasmus to baseline. Bonus: case75 (section-wide bag, its own row) improved TxtBnd 11.1→33.3 from the same parse. Full suite: 208 scored, 0 regressions (197 unchanged; uk_commercial_lease sub-pixel hash shift only, footnotes byte-identical decimal). ponytail: single doc-wide format taken from the first section that names one — per-section formats in multi-section docs unexercised; doc-wide settings.xml bag intentionally NOT honored (Word ignores it). |
| case75 | `w:footnotePr`/`w:endnotePr` **section-wide** note mark numbering format in body `w:sectPr` (§17.11.11/.5, `w:numFmt`) | DONE | (pre-case74) plain decimal → 66.1/83.6 (TxtBnd 100) | NO new code needed — already delivered by case74's commit 48ef128. case74's investigation proved Word reads the note mark `numFmt` from the SECTION-level `w:sectPr/footnotePr|endnotePr/numFmt` (not the doc-wide `settings.xml` bag), so it parses `footnote_num_fmt`/`endnote_num_fmt` on `SectionProperties` (`src/docx/sections.rs`) and formats marks via `numbering::format_number` in the `src/pdf/mod.rs` pre-pass — which is EXACTLY the §17.11.11/.5 feature case75 isolates. Verified genuine (not coincidence): case75's bag lives ONLY in the body `w:sectPr` (`<w:footnotePr><w:numFmt w:val="upperRoman"/></w:footnotePr>` + `<w:endnotePr><w:numFmt w:val="lowerLetter"/></w:endnotePr>`), NO `settings.xml` bag; generated AND reference both render footnotes `I`/`II`/`III` (upperRoman) and endnotes `a`/`b` (lowerLetter). Crosses both thresholds (Jaccard 66.1 ≥ 20, SSIM 83.6 ≥ 75, TxtBnd 100); not in SKIPLIST. No code change this run → nothing to commit, no regression risk. ponytail: single section's format read; multi-section per-section override + `pos`/`numStart`/`numRestart` still deferred (case76 covers `pos`). |
| case76 | `w:pos` footnote placement (§17.11.21, ST_FtnPos `beneathText`) — doc-wide settings.xml footnotePr bag | DONE | already 77.7/94.3 (TxtBnd 100) | **NO new code needed — the Word `reference.pdf` reveals Word IGNORES the doc-wide `word/settings.xml` footnotePr `pos` (exactly the case74 `numFmt` finding repeated for `pos`).** The fixture places `<w:footnotePr><w:pos w:val="beneathText"/></w:footnotePr>` in `settings.xml` (the sectPr is empty); Maggie predicted the 2 footnotes would hug the text block near the page top. But mutool stext on the reference (page h=792) shows the footnotes at y≈698–719 from the top = ~73pt from the bottom (the 1" bottom margin) — Word painted them at the DEFAULT `pageBottom`, NOT beneath the text. docxside already pins footnotes to the page-bottom margin (`render_page_footnotes`, `src/pdf/footnotes.rs`), so generated and reference already match (Jaccard 77.7 ≥ 20, SSIM 94.3 ≥ 75, TxtBnd 100). Implementing `beneathText` against THIS fixture would REGRESS the score (it would move the footnotes up, away from where Word actually placed them). So the genuine `beneathText`/`sectEnd` placement feature stays unexercised by any Word reference — it would need the `pos` in a section-level `w:sectPr` (the case74/75 lesson: Word reads the note bag from `sectPr`, not `settings.xml`) AND a Word that honors it there. Not in SKIPLIST; runs in the suite. No code change → nothing to commit, no regression risk. ponytail: don't implement an unverifiable placement mode that the only available reference contradicts. |
| case70 | `w:lnNumType` section-level margin line numbering (§17.6.8, `@countBy`/`@start`/`@distance`/`@restart`) | DONE | — / — → 76.3/94.1 (TxtBnd 100) | Margin line numbers were entirely unmodeled. Added `LineNumbering` + `LineNumberRestart` to `SectionProperties` (`src/model/mod.rs`); parsed `w:lnNumType` (`@countBy` default 1, `@start` default 1, `@distance` twips→pt, `@restart` continuous/newSection/newPage) in `src/docx/sections.rs`. Rendering: `render_paragraph_lines` (`src/pdf/layout.rs`) gained a `LineNumberArg` param; for each counted body line it advances a shared `line_number_counter` on `LayoutState` (`src/pdf/mod.rs`) and draws the value RIGHT-aligned at `margin_left − distance` (default 0.25"/18pt) on that line's own baseline, in the line's font/size — matching Word. `@countBy` gates which numbers show (`value % countBy == 0`). KEY detail: Word's `continuous` restart treats `@start` as the *previous* section's last line, so the first line displays `start + 1` (hence the reference's 2…13, not 1…12) — modeled via `continuous_offset`. All `render_paragraph_lines` callers (footnotes/header_footer/table/textbox_render) pass `None`. `newPage`/`newSection` counter resets ponytail-deferred (only `continuous` exercised); per-paragraph `w:suppressLineNumbers` out of scope. Crosses both thresholds. Full suite: no regressions (197 unchanged). |
