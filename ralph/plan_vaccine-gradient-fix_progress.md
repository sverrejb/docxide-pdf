# Progress for ralph/plan_vaccine-gradient-fix.md

## Task 1: Modify `src/pdf/mod.rs` — Gradient rendering ✅

**Completed**: Added `srgb_to_linear` helper, updated gradient color interpolation to use linear-light blending, changed shading color space from DeviceRGB to CalRGB with sRGB primaries matrix and gamma=1.0 (linear).

Changes made:
- Added `srgb_to_linear(c: u8) -> f32` function (IEC 61966-2-1 transfer function)
- Updated 2-stop gradient exponential function to use `srgb_to_linear()` instead of `c / 255.0`
- Updated multi-stop (stitching) gradient sub-functions similarly
- Changed shading color space from `.device_rgb()` to `.cal_rgb()` with D65 white point and sRGB primaries-to-XYZ matrix

Build: compiles cleanly. Tests: zero regressions. Vaccine case Jaccard stable at 41.1% (-0.1pp).

## Task 2: Create `tests/fixtures/cases/case38/generate.py` — Gradient test fixture ✅

**Completed**: Created gradient test fixture with 6 diverse shapes testing linear-light blending.

Shapes:
1. Blue→Orange (90°) — vaccine case gradient, should show pink midtone
2. Red→Cyan (0°) — maximum hue shift, most sensitive to interpolation space
3. Red→Green→Blue 3-stop (90°) — tests stitching function path
4. Black→White (90°) — control case, identical in both blending spaces
5. Dark Blue→Bright Yellow (45°) — large luminance range
6. Purple→Orange (180°) — cross-hue blend

Layout: 3×2 grid of rect shapes, each 1.8"×1.4", with labeled text. Uses python-docx + ZIP post-processing pattern (same as case34) with `a:gradFill` instead of `a:solidFill`.

Generated `input.docx` verified: all 6 gradients present with correct `a:gsLst` stops and `a:lin` angles. Converter processes it without errors. Reference PDF must be generated by user via MS Word.
