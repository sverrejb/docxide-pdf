Phase 1, Step 1: DONE — Created src/geometry/ module with types, formula evaluator, path commands.
  - src/geometry/mod.rs: Public API (evaluate_preset, evaluate_def, EvaluatedShape, EvaluatedPath)
  - src/geometry/formulas.rs: 17-operator formula evaluator (FormulaOp enum, GuideEnv with built-in variables)
  - src/geometry/path.rs: PathCommandDef, ResolvedCommand, resolve_path(), arcTo→cubic, quadBez→cubic
  - src/geometry/definitions.rs: PresetDef/TextRectDef types, placeholder lookup() (to be filled in Step 4)
  - 24 unit tests passing (all 17 operators, built-ins, path resolution, y-flip, coordinate scaling, arc conversion)
  - Zero regressions in full test suite

Phase 1, Step 2: DONE — Comprehensive unit tests for all 17 operators + built-in variables.
  - Added test_tan: verifies Tan operator (1000 * tan(45°) ≈ 1000)
  - Added test_sqrt_negative: verifies sqrt of negative returns 0
  - Added test_add_div_zero_denom: verifies divide-by-zero returns 0
  - Added test_if_else_zero: verifies x==0 takes else branch (spec: x>0 for true)
  - Added test_builtin_wd_divisors: all 8 wd{n} values (wd2..wd12)
  - Added test_builtin_hd_divisors: all 7 hd{n} values (hd2..hd10)
  - Added test_builtin_ssd_divisors: all 6 ssd{n} values (ssd2..ssd32)
  - Added test_builtin_all_angle_constants: all 7 angle constants (cd2..7cd8)
  - 32 unit tests passing, zero regressions

Phase 1, Step 3: DONE — Already completed as part of Step 1.
  - arc_to_cubics() in geometry/path.rs (lines 199-264): OOXML arcTo → cubic bezier segments
  - QuadBezTo → CubicTo conversion in resolve_path() (lines 137-153)
  - Both covered by existing tests: test_arc_90_degree, test_quad_to_cubic_conversion
  - Connector arc rendering in pdf/mod.rs kept separate (unification deferred to Phase 5)

Phase 1, Step 4: DONE — Hand-coded definitions for all 11 shapes in fixtures.
  - src/geometry/definitions.rs: Complete preset definitions with guides, paths, text rects
  - Simple shapes: rect, line, straightConnector1 (no guides, direct paths)
  - Medium shapes: triangle (4 guides), roundRect (7 guides), corner (12 guides),
    notchedRightArrow (10 guides), ellipse (6 guides, 4 arcTo)
  - Complex shapes: arc (42 guides, 2 paths: fill-only + stroke-only),
    circularArrow (~155 guides), leftCircularArrow (~155 guides)
  - All definitions sourced from ECMA-376 presetShapeDefinitions.xml (Apache POI repo)
  - Added PathFill::is_filled() helper method
  - 14 new tests: shape registration, command counts, path properties, text rects, adjustments
  - 46 total geometry tests passing, zero regressions in full test suite

Phase 1, Step 5: DONE — Unit tests comparing geometry engine output against hand-coded paths.
  - test_rect_matches_handcoded: verifies rect vertices match content.rect() corners after y-flip
  - test_ellipse_matches_handcoded: verifies 4 arc segments hit cardinal points and cubic
    control points match the K=0.5522847498 approximation (within 0.5pt tolerance)
  - test_notched_right_arrow_matches_handcoded: verifies all 8 vertices exactly match
    hand-coded arrow (w=200, h=100, default adj1=adj2=50000)
  - BUG FIXED: resolve_path() was not scaling from EMU guide space back to point space
    when PathDef.w/h were None. Added coord_w/coord_h parameters to resolve_path() so
    guide values (in EMU) are properly converted to output points.
  - 49 total geometry tests passing, zero regressions in full test suite

Phase 2, Step 1: DONE — Added ShapeGeometry and CustomGeometry types to model.rs.
  - ShapeGeometry: preset name (Option<String>), adjustments (Vec<(String, i64)>), custom (Option<CustomGeometry>)
  - Default is preset "rect" with no adjustments (matches old ShapeType::Rect default)
  - CustomGeometry: owned types for runtime-parsed a:custGeom (adjust_defaults, guides, paths)
  - CustomGuideDef: owned GuideDef equivalent (String fields instead of &'static str)
  - CustomPathDef: owned PathDef equivalent with CustomPathCommand enum
  - CustomPathCommand: MoveTo, LineTo, ArcTo, CubicBezTo, QuadBezTo, Close (all with String params)
  - ShapeType enum kept in place — will be replaced by callers in Phase 2 Steps 2-3
  - Zero regressions in full test suite

Phase 2, Step 2: DONE — Updated SmartArt + textbox parsing to produce ShapeGeometry.
  - SmartArt (src/docx/smartart.rs): Replaced ShapeType match with parse_shape_geometry()
    that extracts preset name + a:avLst adjustments into ShapeGeometry
  - Textbox (src/docx/textbox.rs): Replaced limited prstGeom match with parse_shape_geometry()
    that handles both a:prstGeom (preset name + avLst) and a:custGeom (guides + paths)
  - Added parse_shape_geometry() pub(super) helper: parses spPr node for prstGeom or custGeom
  - Added parse_avlst(): extracts adjustment values from a:avLst (name, "val N" formula → i64)
  - Added parse_custom_geometry(): full a:custGeom parser (avLst, gdLst with FormulaOp, pathLst)
  - Added parse_path_commands(): parses moveTo/lnTo/arcTo/cubicBezTo/quadBezTo/close from a:path
  - Added FormulaOp::from_str() to geometry/formulas.rs: maps OOXML formula names to FormulaOp
  - Model fields changed: SmartArtShape.shape_type and Textbox.shape_type → ShapeGeometry
  - Rendering (pdf/smartart.rs, pdf/mod.rs): draw_shape_path and render_shape_fill now take
    &ShapeGeometry, temporarily mapping preset names to old hand-coded behavior
  - VML textbox fallback uses ShapeGeometry::default() (rect)
  - ShapeType enum now has zero usages — kept for Phase 3 cleanup
  - 49 geometry tests + all integration tests passing, zero regressions

Phase 2, Step 3: DONE — Updated draw_shape_path and render_shape_fill to use geometry engine.
  - src/pdf/smartart.rs: Replaced hand-coded ellipse/notchedRightArrow/rect match with geometry
    engine dispatch. draw_shape_path now calls evaluate_preset() or evaluate_custom(), then emits
    ResolvedCommand paths to the PDF content stream via emit_evaluated_paths().
  - Added evaluate_shape_geometry() helper: dispatches to geometry::evaluate_custom() for
    a:custGeom or geometry::evaluate_preset() for preset shapes, falls back to PDF rect.
  - Added emit_evaluated_paths(): translates ResolvedCommand (MoveTo/LineTo/CubicTo/Close)
    to pdf-writer content stream calls with (x, y) offset translation.
  - src/geometry/mod.rs: Added evaluate_custom() for CustomGeometry (a:custGeom) support.
    Uses same EMU scaling approach as evaluate_preset, evaluates custom guides and paths.
  - src/geometry/formulas.rs: Added evaluate_custom_guides() to GuideEnv for owned
    CustomGuideDef (String fields vs &'static str in GuideDef).
  - src/geometry/path.rs: Added resolve_custom_path() for CustomPathDef with owned
    CustomPathCommand variants (same resolution logic as resolve_path).
  - render_shape_fill (pdf/mod.rs) unchanged — already delegates to draw_shape_path,
    so it automatically uses the geometry engine now.
  - 49 geometry tests + all integration tests passing, zero regressions

Phase 2, Step 4: DONE — Full test suite verified with zero regressions.
  - cargo test -- --nocapture: all tests pass, no "REGRESSION" lines in output
  - 49 geometry unit tests passing
  - All visual comparison tests pass (Jaccard + SSIM)
  - Small score deltas within normal variation: case3 -1.0pp Jaccard, case3 -0.6pp SSIM,
    case33 -0.1pp both metrics, sample500kB -0.3pp Jaccard, classroom_weekly -0.2pp SSIM
  - No delta exceeds regression threshold — zero regressions confirmed
  - Phase 2 (Model migration + rendering integration) is now COMPLETE

Phase 3, Step 1: DONE — Code generator tool for presetShapeDefinitions.xml → definitions.rs.
  - Downloaded presetShapeDefinitions.xml from Apache POI (ECMA-376 Annex D) to tools/data/
  - Created tools/src/bin/generate_shapes.rs: parses XML, emits complete Rust definitions module
  - Added generate-shapes binary to tools/Cargo.toml
  - XML parsing: extracts avLst (adjustment defaults), gdLst (guide formulas), pathLst (path commands),
    rect (text rectangle) for each of 187 shape elements
  - Formula mapping: all 17 operators (*/, +-, +/, ?:, abs, sqrt, min, max, pin, sin, cos, tan,
    at2, cat2, sat2, mod, val) mapped to FormulaOp enum variants
  - Path command support: moveTo, lnTo, arcTo, cubicBezTo, quadBezTo, close
  - Path attributes: fill (none→PathFill::None, default→Norm), stroke (false→false, default→true),
    w/h (optional coordinate system override)
  - Output includes: struct defs (TextRectDef, PresetDef), gd! macro, lookup() match with 187 arms,
    named statics for each shape's commands/paths/guides/preset
  - Verified: generates 9748 lines of Rust, compiles cleanly, all 187 shapes in lookup,
    output matches hand-coded rect/roundRect/ellipse/arc/line definitions exactly
  - Usage: generate-shapes [input.xml] [output.rs] (defaults to stdout)

Phase 3, Step 2: DONE — Generated all 187 definitions, replaced hand-coded Phase 1 definitions.
  - Ran generate-shapes to produce 9748-line definitions.rs from presetShapeDefinitions.xml
  - Replaced hand-coded src/geometry/definitions.rs (11 shapes, 1024 lines) with generated version (187 shapes)
  - All struct definitions (TextRectDef, PresetDef), gd! macro, and lookup() are now auto-generated
  - Preserved all existing unit tests, added test_187_shapes_registered to spot-check across alphabet
  - Updated test_line_is_stroke_only → test_line_has_two_commands (XML lacks fill="none" for line;
    hand-coded version overrode this, generated version matches spec default of Norm)
  - Fixed generator to not emit #![allow(clippy::all)] (inner attribute invalid in module files)
  - 50 geometry tests passing, all integration tests passing, zero regressions confirmed

Phase 3, Step 3: DONE — Removed old ShapeType enum entirely.
  - Deleted ShapeType enum (Rect/Ellipse/NotchedRightArrow) from src/model.rs (lines 175-181)
  - Had zero code usages — fully replaced by ShapeGeometry in Phase 2
  - Updated roadmap.md to mark "More shape types" as DONE
  - Compilation verified clean (cargo check), no regressions

Phase 4, Step 1: DONE — case34 preset shape gallery test fixture.
  - Created tests/fixtures/cases/case34/generate.py: python-docx + ZIP post-processing
  - 20 preset shapes in a 4×5 grid as floating wp:anchor wps:wsp elements
  - Shapes: roundRect, diamond, chevron, pentagon, hexagon, triangle, rightArrow, heart,
    star5, cloud, donut, flowChartProcess, flowChartDecision, flowChartTerminator, plus,
    trapezoid, parallelogram, frame, leftArrow, upDownArrow
  - Each shape has a:prstGeom with default adjustments, a:solidFill with distinct color,
    a:ln stroke, and centered text label inside wps:txbx
  - Generated PDF shows all 20 shapes rendering correctly via geometry engine
  - No reference.pdf yet — user will generate via MS Word
  - 50 geometry tests passing, all integration tests passing, zero regressions

Phase 4, Step 2: DONE — case35 adjusted shapes test fixture.
  - Created tests/fixtures/cases/case35/generate.py: python-docx + ZIP post-processing
  - 20 shapes in a 2-column × 10-row layout: each row shows default (left) vs adjusted (right)
  - 10 shape types with non-default avLst overrides:
    roundRect adj=40000 (rounder corners), star5 adj=35000 (shallower star),
    rightArrow adj1=25000/adj2=75000 (narrow head, thin shaft),
    hexagon adj=10000 (very tapered), trapezoid adj=10000 (nearly rectangular),
    plus adj=10000 (thin cross), donut adj=10000 (thin ring),
    frame adj1=30000 (thick frame), chevron adj=25000 (narrow chevron),
    triangle adj=80000 (skewed right)
  - Each shape has a:prstGeom with a:avLst containing a:gd override formulas
  - 11 total fmla="val N" entries (rightArrow has 2 adjustments)
  - PDF generation successful, shapes render with visibly different proportions
  - No reference.pdf yet — user will generate via MS Word
  - All integration tests passing, zero regressions

Phase 4, Step 3: DONE — case36 custom geometry test fixture.
  - Created tests/fixtures/cases/case36/generate.py: python-docx + ZIP post-processing
  - 6 shapes in a 3×2 grid, each using a:custGeom instead of a:prstGeom
  - Shape types exercising different path command features:
    1. Star (moveTo + lnTo + close) — 6-pointed star with literal coordinates
    2. Heart (moveTo + cubicBezTo + close) — heart using 4 cubic bezier curves
    3. Arrow (guides + avLst) — block arrow with gdLst formulas and adjustment defaults
    4. Wave (cubicBezTo + lnTo mixed) — S-wave ribbon shape
    5. Cross (guides + avLst) — plus/cross with guide-computed arm positions
    6. Rounded Rect (lnTo + arcTo) — rectangle with arcTo corner arcs
  - Shapes 3 and 5 use a:avLst + a:gdLst with formula operators (*/, +-)
  - Shape 6 tests arcTo commands (90° arcs at each corner)
  - Shapes 1, 2, 4 use explicit path w/h coordinate systems (10000×10000)
  - Shapes 3, 5, 6 use built-in variables (w, h, vc, hc) without explicit path w/h
  - All 6 shapes render correctly in generated PDF
  - No reference.pdf yet — user will generate via MS Word
  - 50 geometry tests + all integration tests passing, zero regressions

Phase 4, Step 4: DONE — case37 SmartArt with diverse shapes test fixture.
  - Created tests/fixtures/cases/case37/generate.py: python-docx + ZIP post-processing
  - Inline SmartArt diagram using dsp: namespace (word/diagrams/drawing1.xml)
  - Tests SmartArt-specific parsing path (dsp:sp elements) vs floating wps:wsp path in case34-36
  - 2-row process diagram layout:
    Row 1: 5 main shapes + 4 rightArrow connectors in a process flow
    Row 2: 4 additional diverse shapes
  - 8 distinct preset types: ellipse, roundRect (adj=25000), diamond, pentagon,
    rightArrow, hexagon, chevron, trapezoid, flowChartProcess
  - 13 total shapes with fills, strokes, and centered text labels
  - ZIP structure includes: word/diagrams/drawing1.xml, document.xml.rels diagram
    relationship, Content_Types override for diagram drawing
  - All shapes render correctly through geometry engine via SmartArt code path
  - No reference.pdf yet — user will generate via MS Word
  - 50 geometry tests + all integration tests passing, zero regressions

Phase 4, Step 5: PARTIAL — Visual comparison for case34 and case36 (have reference PDFs).
  - case34 (preset shape gallery): 94.7% Jaccard, 80.4% SSIM — both passing
  - case36 (custom geometry): 96.7% Jaccard, 91.4% SSIM — both passing
  - Baselines already stored in tests/baselines.json
  - BLOCKED: case35 and case37 still need reference.pdf generated via MS Word
